
services:
  risk-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: razorpay-risk-manager
    ports:
      - "8000:8000"
    volumes:
      - ./audit.db:/app/audit.db
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
