# Core ML
lightgbm>=4.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Vision — CPU-only build recommended (no GPU assumed for this project)
# If torch install fails or pulls CUDA wheels, install manually first with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
Pillow>=10.0.0
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.36.0

# segment-anything-2 is commented out; uncomment the GitHub install for production.

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Evaluation & plotting
matplotlib>=3.7.0
seaborn>=0.13.0

# Utilities
tqdm>=4.65.0
requests>=2.31.0
joblib>=1.3.0
